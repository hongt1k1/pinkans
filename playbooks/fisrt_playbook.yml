- name: Network getting started first play book
  connection: network_cli
  gather_facts: false
  hosts: all
  tasks:
    - name: Get config for Pink OS devices
      vyos_facts:
        gather_subset: all
    - name: Display the config
      debug: 
      msg: "The hostname is {{ ansible_net_hostname}} and the OS is {{ ansible_net_version }}"
